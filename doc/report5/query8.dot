graph{
    Nested Loop (supplier.s_nationkey = n2.n_nationkey) -- Materialize1;
    Nested Loop (supplier.s_nationkey = n2.n_nationkey) -- Hash Join (lineitem.l_suppkey = supplier.s_suppkey);
    Materialize1  -- nation n2;
    Hash Join (lineitem.l_suppkey = supplier.s_suppkey) -- Hash1;
    Hash Join (lineitem.l_suppkey = supplier.s_suppkey) -- Hash Join (lineitem.l_partkey = part.p_partkey)
    Hash1 -- supplier;
    Hash Join (lineitem.lpartkey = part.p_partkey) -- Hash2;
    Hash Join (lineitem.lpartkey = part.p_partkey) -- Hash Join (lineitem.l_orderkey = orders.o_orderkey);
    Hash2 -- Fileter (p_type = 'ECONOMY ANODIZED STEEL');
    Fileter -- part;
    Hash Join (lineitem.l_orderkey = orders.o_orderkey) -- lineitem;
    Hash Join (lineitem.l_orderkey = orders.o_orderkey) -- Hash3;
    Hash3 -- Hash Join (orders.o_custkey = customer.c_custkey);
    Hash Join (orders.o_custkey = customer.c_custkey) -- Filter (1995-01-01 <= o_orderdate <= 1996-12-31);
    Filter (1995-01-01 <= o_orderdate <= 1996-12-31) -- orders;
    Hash Join (orders.o_custkey = customer.c_custkey) -- Hash4;
    Hash4 -- Hash Join (customer.c_nationkey = n1.n_nationkey);
    Hash Join (customer.c_nationkey = n1.n_nationkey) -- customer;
    Hash Join (customer.c_nationkey = n1.n_nationkey) -- Hash5;
    Hash5 -- Hash Join (n1.n_regionkey = region.r_regionkey);
    Hash Join (n1.n_regionkey = region.r_regionkey) -- nation n1;
    Hash6 -- Filter (r_name = 'AMERICA');
    Filter (r_name = 'AMERICA') -- region
}
